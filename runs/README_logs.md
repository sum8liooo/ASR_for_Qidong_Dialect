# Training / evaluation logs

`train.log` files are verbatim training logs with local filesystem paths replaced
by `<HOME>` and `<REPO>` placeholders. `eval.log` files are withheld because they
contain per-utterance reference and hypothesis transcripts, which form part of the
corpus (see Data availability).
